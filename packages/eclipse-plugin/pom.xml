<?xml version="1.0" encoding="UTF-8"?>
<!-- 한국어 주석: Moli Code Eclipse 플러그인을 Tycho로 빌드하는 루트 집계 POM입니다. -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.moli.code.eclipse</groupId>
  <artifactId>com.moli.code.eclipse.parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <tycho.version>1.7.0</tycho.version>
    <!-- 한국어 주석: 테스트 검증된 최소 타겟: Eclipse 2020-09 (4.17), Java 8 호환 -->
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
  </properties>

  <modules>
    <module>com.moli.code.eclipse.core</module>
    <module>com.moli.code.eclipse.ui</module>
    <module>com.moli.code.eclipse.feature</module>
  </modules>

  <repositories>
    <repository>
      <id>eclipse-2020-09</id>
      <layout>p2</layout>
      <url>https://download.eclipse.org/releases/2020-09</url>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>eclipse-2020-09</id>
      <layout>p2</layout>
      <url>https://download.eclipse.org/releases/2020-09</url>
    </pluginRepository>
  </pluginRepositories>

  <build>
    <plugins>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-maven-plugin</artifactId>
        <version>${tycho.version}</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>target-platform-configuration</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <environments>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>aarch64</arch>
            </environment>
            <environment>
              <os>linux</os>
              <ws>gtk</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>win32</os>
              <ws>win32</ws>
              <arch>x86_64</arch>
            </environment>
          </environments>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
